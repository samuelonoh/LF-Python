substitute = "{} {} {} {} {}"

print(substitute.format(
    "Pick a seed",
    "Pick up the dice",
    "Toss it to the table",
    "Toss it twice",
    "Then move the seed"
    ))

print(substitute.format(True, False, True, False, False))